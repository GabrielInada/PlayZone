export enum MatchStatus {
  SCHEDULED = 'SCHEDULED',
  FINISHED = 'FINISHED',
  CANCELED = 'CANCELED',
}

export enum ReportStatus {
  PENDING = 'PENDING',
  VALIDATED = 'VALIDATED',
  REJECTED = 'REJECTED',
}

export enum Role {
  ADMIN = 'ADMIN',
  DELEGADO = 'DELEGADO',
  CLUBE = 'CLUBE',
}